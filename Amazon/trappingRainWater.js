/**
 * Sliding Window
 * Time: O(n)
 * Space: O(1)
 */
var trap = function(height) {
    let [l, r] = [0, 1];
    let max = 0;
    while (r < height.length) {
    
    }
    return max;
};
console.log(trap(height = [4,2,0,3,2,5]))