var insertIntoBST = function(root, val) {
    let masterRoot = Object.assign(...root);

    const insert = (node) => {
        if (!node) return;
    }
};


/**
 * 1. Insert the node in the correct position.
 * 2. Keep track of the root node.
 * 3. Re-organize the tree after insertion....keep 
 */