/**
 * @param {number[]} nums
 * @return {number}
 */
var maxSubArray = function(nums) {
    let prev1 = nums[0] >= -Infinity ? nums[0] : -Infinity
    let prev2 = -Infinity

    for (let i = 1;i <= nums.length; i++) {

    }
};