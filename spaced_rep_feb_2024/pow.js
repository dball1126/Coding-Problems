/**
 * @param {number} x
 * @param {number} n
 * @return {number}
 */
var myPow = function(x, n) {
    let pows = 2, result = 0, powN = n
    while (pows < n) {
        if (pows * 2 <)
        powN *= pows
        
    }
    return result
};