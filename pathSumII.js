/**
 * Use DFS. Recursion and or Stack.
 * Time Complexity: (N) N is the number of nodes
 * Space Complexity: (N) N is the number of nodes
 */

var pathSum = function(root, targetSum) {
    let [stack]
};