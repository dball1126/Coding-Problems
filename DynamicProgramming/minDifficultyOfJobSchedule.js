/**
 * Intuition: Calculate the valid max for every valid scenario
 *            Iterate over the DP and return the minimum of the valid scenarios
 * 
 * State Var: i for idx of jobs, j for the amount of days that are remaining
 * Base case: 0 if jobs.length less than days. 0 if out of bounds
 * Recurrence Releation:
 *      dp(i, j) = 
 */

var minDifficulty = function(jobDifficulty, d) {
    
};