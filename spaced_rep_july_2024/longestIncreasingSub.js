/**
 * @param {number[]} nums
 * @return {number}
 */
var lengthOfLIS = function(nums) {
    const n = nums.length

    const dp = [...new Array(n+1)].fill(1)
    let max = 1

    for (let i = 0; i < n; i++) {
        
    }
};